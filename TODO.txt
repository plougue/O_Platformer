* Add a superclass Item that has the member methods GetPosition() DeclareCollision() GetSize() for every physical object in the game
(Parent class of Obstacle, Character, Projectile, class used for the CheckCollision() function )

* Think about separating hitbox from image size
